# Copy this file to `.env.local` for local development if you introduce runtime vars.
# Currently no map token is required (Leaflet + OSM tiles).
# Example placeholders if you later add APIs:
# NEXT_PUBLIC_API_BASE=https://api.example.com
